model:
  _target_: methods.random_method.model
  method_cfg: ${method.parameters}
name: random
parameters:
  desired_compression: 0.1
  token_compression: null
  compression_enabled: true
  channel_eval_only: false
  semantic_waterfilling: true

  # Optional fixed number of patch tokens at eval (null = same ratio as train)
  eval_k: null